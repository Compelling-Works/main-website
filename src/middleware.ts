export { default } from "next-auth/middleware";

export const config = {
  matcher: [
    // "/admin",
    // "/admin/projects",
    // "/admin/publications",
    // "/admin/team",
    // "/admin/users",
  ],
};
